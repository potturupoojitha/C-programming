
#include <stdio.h>

int main() {
    int n, i;
    float num, sum = 0, avg;

    printf("Enter count: ");
    scanf("%d",&n);

    for(i=0;i<n;i++){
        scanf("%f",&num);
        sum += num;
    }

    avg = sum / n;

    printf("Average = %.2f", avg);

    return 0;
}
