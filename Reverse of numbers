#include <stdio.h>
#include <string.h>
int main() {
    char str[]="hello";
    int n=strlen(str);

    // Left shift
    char first=str[0];
    for(int i=0;i<n-1;i++)
        str[i]=str[i+1];
    str[n-1]=first;

    printf("Left Shift: %s\n", str);

    return 0;
}
