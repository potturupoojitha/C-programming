
#include <stdio.h>

int main() {
    int n, i = 0;
    char hex[20];

    printf("Enter a decimal number: ");
    scanf("%d", &n);

    while(n > 0) {
        int rem = n % 16;

        if(rem < 10)
            hex[i] = rem + '0';
        else
            hex[i] = rem - 10 + 'A';

        n = n / 16;
        i++;
    }

    printf("Hexadecimal equivalent: ");
    for(int j = i)
}
}
