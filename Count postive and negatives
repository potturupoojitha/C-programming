
#include <stdio.h>

int main() {
    int n, i, num, pos=0, neg=0;

    scanf("%d",&n);

    for(i=0;i<n;i++){
        scanf("%d",&num);

        if(num >= 0)
            pos++;
        else
            neg++;
    }

    printf("Positive = %d\nNegative = %d", pos, neg);

    return 0;
}
